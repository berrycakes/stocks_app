-if !current_user.nil?
    %h1= "Hello #{current_user.trader.first_name}"
    %p= "Buying Power #{current_user.trader.wallet.balance}"
    %p= current_user.trader.profit_loss
-else 
    %h1 Hello Bes

